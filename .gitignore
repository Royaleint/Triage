# Dev-only documents and working files
Triage_Dev/
AGENTS.md
CLAUDE.md
Tri_Tracker.md
Tri_Completed.md
.codex/skills/triage/

# Third-party addon source kept locally for pattern study only — must never
# ship or be copied into Triage code (data-contamination rule). See TRI-039.
dbm-research/

# Git worktrees (studio standard — live inside the repo, never committed)
.worktrees/

# Claude Code config (local)
.claude/

# MCP config (copied from BawrLabs, not committed)
.mcp.json

# Binaries
luacheck.exe

# Credentials
.env

# Python
__pycache__/
*.pyc

# Downloaded archives
*.zip

# Generated at CI time by the release workflow (extracted from CHANGELOG.md
# and pandoc-converted to HTML for CurseForge compatibility)
RELEASE_NOTES.md
RELEASE_NOTES.html

# Copy/backup files
*- Copy.lua
*_backup.lua
*_backup_*.lua

# Windows junk
nul
Thumbs.db

# Libraries (fetched by packager via .pkgmeta externals)
# Committed for now since ERF shipped embedded libs.
# Switch to externals-only when pkgmeta packaging is verified.
# Libs/
